
interface Props {
    className?: string;
}

export const SignUp = ({ className }: Props) => {

    return (
        <div className={className} id="sign-up-page">
            Sign Up
        </div>
    );
}
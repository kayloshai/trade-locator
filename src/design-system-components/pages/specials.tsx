
interface Props {
    className?: string;
}

export const Specials = ({ className }: Props) => {

    return (
        <div className={className} id="specials-page">
            Specials
        </div>
    );
}
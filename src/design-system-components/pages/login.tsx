
interface Props {
    className?: string;
}

export const Login = ({ className }: Props) => {

    return (
        <div className={className} id="login-page">
            Login
        </div>
    );
}
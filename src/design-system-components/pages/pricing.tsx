
interface Props {
    className?: string;
}

export const Pricing = ({ className }: Props) => {

    return (
        <div className={className} id="pricing-page">
            Pricing
        </div>
    );
}
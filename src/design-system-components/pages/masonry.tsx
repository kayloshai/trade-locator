
interface Props {
    className?: string;
}

export const Masonry = ({ className }: Props) => {

    return (
        <div className={className} id="masonry-page">
            Masonry
        </div>
    );
}
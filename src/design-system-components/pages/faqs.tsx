
interface Props {
    className?: string;
}

export const FAQs = ({ className }: Props) => {

    return (
        <div className={className} id="faqs-page">
            FAQ's
        </div>
    );
}
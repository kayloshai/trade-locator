
interface Props {
    className?: string;
}

export const Carpentry = ({ className }: Props) => {

    return (
        <div className={className} id="carpentry-page">
            Carpentry
        </div>
    );
}